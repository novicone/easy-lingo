{
  "name": "@easy-lingo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "clean": "rm -rf dist",
    "prebuild": "cd ../.. && pnpm --filter @easy-lingo/web build",
    "build": "node build.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/static": "^7.0.4",
    "fastify": "^4.23.0",
    "papaparse": "^5.5.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/papaparse": "^5.5.2",
    "esbuild": "^0.27.3",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
